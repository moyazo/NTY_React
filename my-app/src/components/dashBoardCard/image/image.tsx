import React, { FC } from "react";
import type { Props } from '../type';
import './description.css';

const Image: FC<Props>  = ({children}) => {
    return (
        <image className='detailUrl'>
            
        </image>
    )
}

export default Image;