import React, { FC } from "react";
import type { Props } from './type';
import dashboard from "../../services/dashboard";

const Card: FC<Props>  = ({children}) => {
    return (
        <div className='textDetail'>HOLA</div>
    )
}

export default Card;